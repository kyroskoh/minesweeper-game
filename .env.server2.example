# Server 2 Configuration
# Copy this to .env.server2 and update with your actual values
# NEVER commit .env.server2 to git!

# Server Configuration
PORT=3030
NODE_ENV=production

# Daily Puzzle Seed Salt (MUST BE IDENTICAL TO SERVER 1!)
DAILY_SEED_SALT=your-secret-salt-here-change-this

# PostgreSQL Configuration
# Point to the SAME central PostgreSQL server as Server 1
POSTGRES_HOST=your-db-server-hostname-or-ip
POSTGRES_PORT=5432
POSTGRES_DB=minesweeper
POSTGRES_USER=minesweeper_user
POSTGRES_PASSWORD=your-secure-database-password

# Connection Pool
POSTGRES_MAX_CONNECTIONS=20
POSTGRES_IDLE_TIMEOUT=30000

# SSL Configuration (set to true in production)
POSTGRES_SSL=true

# Server Identification (for logging/monitoring)
SERVER_NAME=server2
SERVER_REGION=us-west-1
