# Environment Configuration for DB Server 1
# Copy this to .env.db-server1

# Server IPs (IMPORTANT: Update with your actual IPs)
DB_SERVER1_IP=10.0.1.10
DB_SERVER2_IP=10.0.1.11
COORDINATOR_IP=10.0.1.12

# PostgreSQL Cluster Settings
POSTGRES_DB=minesweeper
POSTGRES_USER=minesweeper_user
POSTGRES_PASSWORD=change-to-secure-password

# Superuser
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=change-superuser-password

# Replication
REPLICATION_PASSWORD=change-replication-password

# Patroni
PATRONI_ADMIN_PASSWORD=change-admin-password
